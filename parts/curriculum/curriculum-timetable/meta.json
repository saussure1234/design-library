{
  "id": "curriculum/curriculum-timetable",
  "name": "開講タイムテーブル（曜日 × 時間コマ・コマ数が曜日ごとに違ってよい表）",
  "type": "curriculum",
  "intent": "「いつ通えるのか」を文章ではなく1枚の表で答え、曜日ごとのコマ数の差と休校日を、読ませずに形で分からせる",
  "when": [
    "曜日（またはクラス）が2〜5列、時間コマが1〜6行に収まるとき",
    "曜日によって開講コマ数・開始時刻が違い、共通の時刻軸で組めないとき",
    "コマ単位に「火・木のみ」のような但し書きが付くとき",
    "休校日・定休日を同じ表の中で言い切りたいとき"
  ],
  "avoid": [
    "全曜日が同じ時刻で開講していて差が無いとき（表にする必要がない。1行のテキストで足りる）",
    "曜日が6列以上になるとき（1列あたりが狭くなり時間表記が折れる。クラス別に2セクションへ割る）",
    "コマごとに1行を超える説明が要るとき（curriculum/curriculum-nested-selector へ）",
    "時刻ではなくカリキュラムの進度を見せたいとき（列＝時期の別パーツへ）"
  ],
  "slots": {
    "sectionId": "セクションのアンカーid（省略可）",
    "eyebrow": "小見出し1行（.h-eyebrow）",
    "headline": "見出し。HTML可。改行制御は <span class='nb'> ＋ <wbr>",
    "lead": "表の前の導入1〜2行（省略可）",
    "days": "曜日の列の配列（2〜5件）。各要素は {day, sub, tone, slots, closed, closedNote}",
    "days[].day": "曜日ラベル（例：火〜金／土）。列ヘッダの大きい文字",
    "days[].sub": "曜日ラベルの下に出る小さな補足（省略可・空なら行ごと消える）",
    "days[].tone": "列ヘッダの色。空＝主色 / accent＝差し色。差し色は1列だけに使う",
    "days[].slots": "その曜日の時間コマの配列。各要素は {time, note}。曜日ごとに件数が違ってよい",
    "days[].slots[].time": "時間の表記（例：16:00-16:45）。左の丸数字は配列順から自動で入る",
    "days[].slots[].note": "そのコマだけの但し書き（例：火・木のみ）。10文字程度まで。空なら消える",
    "days[].closed": "休校の列にするときだけ入れる文字（例：休校）。入れるとその列は斜線ハッチになり slots は不要",
    "days[].closedNote": "休校ラベルの下の補足1行（省略可）",
    "notes": "表の下の注記の配列（文字列の配列）。頭の「※」はCSSが付けるので原稿には書かない",
    "photo": "表の下に添える写真のパス（省略可）",
    "photoAlt": "写真の代替テキスト",
    "ctaLabel": "CTAボタンの文言（省略可・空ならボタンごと出ない）",
    "ctaHref": "CTAボタンのリンク先"
  },
  "motion": "列単位で1本ずつ立ち上げる。曜日ヘッダとコマの束に同じ data-d（{{.d}}＝列の順×90ms）を与えているので、見た目は「左の曜日から順に列が入る」。コマは1行ずつ動かさない（時刻は読む対象なので揺らさない）。写真は scale、CTAは最後に fadeup。",
  "responsive": "900px以下で表を解体し「1曜日＝1カード」の縦リストへ。PCは .ttb__col を display:contents にして 2行×N列 の1枚の表に組み直し、SPでは列を実体（カード）に戻すだけ——DOMは曜日ごとの塊のままなので並べ替え不要。SPではコマの「時間＋但し書き」を1行に寄せて縦を詰め、休校列は1行のバッジに畳む。表の下は縦積みになり、写真→注記→CTAの順で必ず写真がCTAより上に来る。横スクロールは使わない。",
  "verified": false,
  "refs": [
    "sho_atama_s08",
    "ikeda_s12",
    "tanq_s05",
    "sho_ripple_s06",
    "sho_kimini_s10"
  ]
}
