# 引き継ぎ（完全版・1ファイル）── LP制作の質を上げる取り組み

最終更新：2026-08-24
渡す相手：Codex または別セッションのClaude
**このファイル1本で完結する。** 他のファイルを開かなくても続きができる。

コードだけは別途必要：
  ~/design-library/docs/esl4/fv/loop.py   （自動検証。16KB）

---


## 0. いま何をやっているか

**目的**：LP制作の質を、案件が変わっても同じ水準で出せるようにする（＝再現性）。

**いま走っている案件は2つ**

| | 状態 |
| --- | --- |
| **ESL club オンライン校**（英検特化の英語塾） | FV 8案が完成。検証済み。**選定待ち** |
| **SHARE Security**（脆弱性診断／宮田さん） | **仮**のLP。素材の棚卸しまで完了。ベンチ撮影中で止まっている |

---

## 1. 確立した作り方（これが本体）

出力が「軽く似せただけ」で止まる原因は3つあった。全部対処が入っている。

```
① ベンチマークを実寸で開く          ← 一覧サムネで判断しない（5本中3本を取り違えた実績あり）
② 数値をスクリプトで実測する         ← 目分量の値をファイルに書かない
③ 禁止事項を先に書く               ← 「何をするか」より「何をしないか」が効く
④ 訴求の重複を、書く前に潰す
⑤ 借りられないものは「借りない」と明記  ← 素材の分量が違えば同じ形は成立しない
⑥ 実装 → loop.py で検証 → スクショを目で見る → 1つ直す → 繰り返す
```

### ★いちばん効いた発見

**回した回数と質が直接相関していた。**

| | 回数 | 結果 |
| --- | --- | --- |
| A・B・C案 | 5〜7回 | 指摘なし |
| D〜H案 | 1〜3回 | 「適当になったやろ」と指摘された |

→ **8案に3回ずつかけるより、1案に20回かけるべき。**
　 候補出しの段階は3周、選ばれた1案だけ20周、が現実的。

### ★実測が必須である証拠

目分量で書いた案（F・H）は、あとで実寸で測ったら読み取りが全部違っていた。

| 目分量で書いたこと | 実測した結果 |
| --- | --- |
| 045「帯は等幅／文字は左の地に置く」 | **不等幅29:43:29／文字は全部写真の上に白抜き／巨大文字が写真をまたぐ** |
| 011「文字は画面内／太め／丸は写真か単色」 | **両端で切れる／細い／同心円／丸は1.05em（私は1.9em＝1.8倍）** |
| 028「帯は斜めの直線／写真は密着」 | **弧／隙間93px／隙間に帯の続きが走る** |

---

## 2. ファイルの場所

```
~/design-library/
├── CRITERIA.md              ★発注者の判定6観点。作る前に必ず読む
├── BENCHMARK_METHOD.md      ベンチマークの選び方（素材の構成で選ぶ）
├── HANDOFF.md               このファイル
├── harvest_sites.csv        1,100サイトの一覧（name,url,source,industry）
├── ds-lp/                   Claude Designに入れたデザインシステムのローカル版
│   ├── SKILL.md             守るべき事実＋禁止事項
│   ├── tokens/              色・級数・余白（各値に実測の根拠をコメントで併記）
│   ├── guidelines/          判定基準4枚（HTMLのプレビューカード）
│   └── examples/            FV 8案（画像を埋め込んだ単体版）
└── docs/
    ├── esl4/                ★ESL club
    │   ├── fv/
    │   │   ├── loop.py      ★自動検証。python3 loop.py A_v05
    │   │   ├── pick.html    8案の選択画面
    │   │   ├── _pick.py     ↑の生成スクリプト
    │   │   ├── _shots/      検証時のスクショ
    │   │   ├── A_v05.html 〜 H_v03.html   ★完成した8案
    │   │   └── 7_v05.html   茗溪型（写真1枚。ユーザーが良いと言った版）
    │   ├── ref/
    │   │   ├── bench/       教育LPのベンチ61本（1画面ぶん）
    │   │   ├── full/        同・フルページ
    │   │   ├── _capture.py / _capture_full.py / _build_picker.py
    │   │   └── picker.html  ベンチの選択画面
    │   └── img/             ESL clubの写真素材
    └── sharesec/            ★SHARE Security（仮）
        ├── MATERIAL.md      ★素材棚卸し。要確認8項目つき
        ├── SLACK_DRAFT.md   確認事項の下書き（★未送信）
        └── ref/
            ├── _list.json   BtoB117/士業16/金融7 = 140本
            ├── _capture.py  撮影スクリプト
            └── bench/       撮影中（78/140で中断）
```

---

## 3. 環境の立て方（毎回これが要る）

セッションが変わると両方落ちている。**先にこれを立てないと何も動かない。**

```bash
# ① 静的サーバー（成果物とベンチを見るため）
cd ~/design-library/docs && python3 -m http.server 8788 &

# ② 撮影用の Chrome（ヘッドレス）
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --user-data-dir=/Users/sohayashi/.chrome-shot2 \
  --remote-debugging-port=9350 \
  --no-first-run --no-default-browser-check --mute-audio --disable-extensions \
  --disable-background-networking --disable-backgrounding-occluded-windows \
  --disable-renderer-backgrounding --disable-background-timer-throttling \
  --window-size=1440,900 --hide-scrollbars about:blank &
```

### ★注意

- **既存の Chrome（ポート9222/9223/9224）を使わないこと。**
  `--blink-settings=imagesEnabled=false --disable-remote-fonts` で起動していて、
  **画像もWebフォントも出ない**（スクレイプ用）。デザインの撮影には使えない
- **ヘッドレスにすること。** 画面外（x=1600など）にウィンドウを置くと
  `Page.captureScreenshot` が無応答になる
- **`deviceScaleFactor` は必ず1。** 2にすると2倍画像になり、
  1倍のつもりで切ると左上1/4だけ見て誤読する（3回やった）
- **`Emulation.clearDeviceMetricsOverride` を毎回先に呼ぶ。** mobileフラグが残る
- **スクリプトごとに専用タブを開く。** 共有すると他のスクリプトが開いた
  ベンチマークを自分のページだと思って測る事故が起きる（実際に起きた）

---

## 4. loop.py（自動検証）

```bash
cd ~/design-library/docs/esl4/fv && python3 loop.py A_v05
```

PC（1440x900）とスマホ（390x844）の両方で、以下を出す。

| 検査 | 合格条件 |
| --- | --- |
| はみ出し | 0px |
| 衝突 | 全ペア総当たり。**祖先-子孫は除外** |
| 画面外 | PCのみ判定（スマホは縦に長いので下方向は普通） |
| 見出し行数 | **全体高÷行送り**で数える。区切りの中で折り返していないか |
| 級数・太さ | 8段以内 |
| ①面と地の段差 | **面があるときだけ**要求。比1.05以上。比較相手は**bodyの地の色** |
| ⑥強調の数 | 3箇所以内（ヘッダーは除外）。CRITERIA.mdは1〜2箇所 |
| 見出しの可読性 | 写真の上なら比3.0以上。**見出しを隠して撮り直した画像**で測る |
| 訴求の重複 | 同じ語が何回出るか（判定はせず一覧を出す） |

### ★loop.py 自体のバグを5つ潰してある。同じ穴を掘り直さないこと

1. **矩形の"数"で行数を数えた** → 丸などのインライン要素で誤検知。全体高÷行送りに変更
2. **白い文字を「背景の明るい側」として拾った** → 見出しを隠して撮り直す方式に変更
3. **親子の重なりを衝突と誤検知** → `a.contains(b)` で除外
4. **他のスクリプトと同じタブを取り合った** → 専用タブを開いて最後に閉じる
5. **画面を覆う大きな面（楕円）が「地」になり、その面自身と比べて必ず1.00になった**
   → 比較相手を bodyの背景色に変更

**数字が全部OKでもスクショを必ず見ること。**
数字は満点なのに写真が左に飛んでいた事故がある（gridにした瞬間DOM順が列順になった）。

---

## 5. 発注者の判定基準（CRITERIA.md の要点）

作る前に `~/design-library/CRITERIA.md` を読むこと。**今回それを20回のあいだ一度も開かなかったのが最大の失敗。**

| # | 観点 | 中身 |
| --- | --- | --- |
| ① | 見えるか | **面と面に段差があるか（輝度差）**。最頻・最再発の指摘 |
| ② | 読めるか | 装飾がコピーの可読性を1mmでも下げたら却下 |
| ③ | 自然か | 動きが図形として意識に上がったら失敗 |
| ④ | 品があるか | **ブランドの色以外を持ち込まない** |
| ⑤ | 動いていないか | 合意済みのレイアウトは不可侵 |
| ⑥ | 過剰でないか | **1セクション内で強調は1〜2箇所まで** |

**触ってはいけない4つ**：コピー／構成・並び順／フォント／要素の増減

---

## 6. 数値の実測メモ（推測ではない。全部PNGから測った）

```
地の輝度       白 0.939 は面が段差を作れない。067=0.867 / 072=0.876 / 茗溪=0.872
              → #E8F3EA（0.872）を既定にした。白カードとの比1.16
濃地           #0A7A2B は輝度0.142。白文字との比5.48（TOPCON実測 0.140/5.52 と同等）
スクリム       白抜きで比3.0 → 背景輝度0.30以下 → 黒の不透明度0.65以上
見出しの級数    幅 ÷ 最長行の字数 × 0.93。先に級数を決めると必ず折り返す
円に文字       内接する正方形は 直径÷√2。直径122pxの円に幅120pxは物理的に入らない
縦書き         display:flex にすると主軸も縦になり行が縦に積まれる。blockのまま使う
茗溪のドット     角丸の四角 約5px、ピッチ約8.7px。円ではない
```

---

## 7. ESL club ─ いまの状態

**FV 8案が完成。全案 PC/スマホとも loop.py で NG 0件。**

| | ベンチマーク | 構成 |
| --- | --- | --- |
| A_v05 | 045 桃山学院大学 | 帯に写真2枚（不等幅57/43）。見出しが写真をまたいで白抜きで横断 |
| B_v07 | 011 UM English Lab. | 純白の地に細い巨大文字。丸は1.05em |
| C_v02 | 028 ひろがり保育 | 上に見出し／下に写真2枚（隙間93px）／右上から弧の帯 |
| D_v01 | 009 Pikkas | 写真が全面。白抜き見出しを中央下。文字間に小さい矩形写真 |
| E_v02 | 046 神戸電子 | タイポ二層。奥に巨大な半透明の数字／手前に見出し |
| F_v02 | 049 Career Palette | 淡いグラデ地。中央上に見出し。下に写真横並び、左上の角に色 |
| G_v02 | 027 TOPCON | ブランド色の濃いベタ地。縦組みを左右に割り中央に写真 |
| H_v03 | 003 日経 for Education | 白地に薄い巨大楕円。傾けた写真2枚。形の違うラベル |

**見るには** `http://localhost:8788/esl4/fv/pick.html`

**素材**：見出しは esl3 から一字一句そのまま（39字）。数字3つ。写真は `p07.jpg`（少年）と `p20.jpg`（中高生）。
どちらも**環境が写っているシーン写真**。切り抜き素材（`fv_boy.webp`等）では
「丸や帯に抜く」が成立しないため差し替えた。

### 次にやること

1. **ユーザーがA〜Hから選ぶ**
2. 選ばれた1案を**20周回す**（`_rounds/` に各周のスクショを残す運用を始めていた）
3. 残り11セクションへ展開。11セクションは**7つの型**に畳める
   （項目の並び／見出し＋本文＋ビジュアル／人物紹介／証言／実績／情報／CTA）

### Claude Design 側

`claude.ai/design` の「Design System」プロジェクトに、
`ds-lp/` の中身（tokens・guidelines・SKILL.md・examples 8案）を**push済み**。
そこで「FV 5案」を生成させたところまで進んだ。**5案は出たが、2〜5案目は未確認。**

- 生成物は別プロジェクト「LP ファーストビュー5案」にある
- **DesignSync は生成できない**（読み書きのみ）。生成は画面操作でしかできない
- Claude Design 側は SKILL.md の「幅÷字数×0.93」を守っていて、
  **こちらが3回踏んだ折り返し事故を最初から避けていた**

---

## 8. SHARE Security ─ いまの状態

**仮のLP。まだ1行もHTMLを書いていない。**

### わかっていること

`~/design-library/docs/sharesec/MATERIAL.md` に全部書いてある。出どころはSlack `#share-security-project`。

要点だけ：

- 宮田さん／SHARE Security社／**脆弱性診断**
- 強み：**既知パターンをAI、固有システムを人**。**固定2名**で見積工数を簡素化。**費用は大手の50%程度**
- ターゲット：製薬・金融と取引のある**SaaS系スタートアップ**、**上場準備（N-2）のCTO**
- ノンターゲット：「診断の実施証明を買う」層／「権威を買う」層
- 業界：AI化はよくて**6割**。経験の要る診断実務に人手が圧倒的に不足
- 言い切れる主張：**「攻撃者が突くのは"仕組み"ではなく"システムの思想"」**

### ★2026-08-24 に追加された札（未確認）

> 宮田さんは自宅にサーバーを置いて、自宅でLLMをローカルで動かせるようにしている。
> 物理的にセキュリティを確保している。それを企業に提案できる。

ユーザーが「多分」と言っているので**事実確認が要る**。ただしSlackには一度も出ていない
＝**別商材**の可能性が高い。ユーザーの判断は「**C（両方）で作る**」。

主題は「**攻撃者の思想を読める人間が、閉じた環境まで設計する**」の一本線。

### 素材の前提が ESL club と真逆

| | ESL club | SHARE Security |
| --- | --- | --- |
| 写真 | シーン写真2枚 | サーバー／機材（**未入手**） |
| 数字 | 3つ | **無い**（実績数字が記録に無い） |
| 主役 | 写真と数字 | **言い切れる主張＝文字** |

→ ベンチマークは選び直し。**教育LP 61本は使えない。**
　 `sharesec/ref/_list.json`（BtoB117/士業16/金融7 = 140本）→ **撮影完了**。
　 選択画面：`http://localhost:8788/sharesec/ref/picker.html`

### 次にやること

1. ~~撮影~~ → **完了済み**。`sharesec/ref/picker.html` で実寸で見られる
2. 140本から「**文字が主役・写真1枚・数字なし**」の構成に一致するものを**実寸で**選ぶ
3. 禁止事項を書く → 作る → loop.py → 20周
4. 写真は**ストックを使わない**。このLPの強さは「自分でやっている実物」なので、
   ストックのデータセンター写真は主張と逆のことをする。
   **ラベル付きのプレースホルダで組み、宮田さんの実物が来たら差し替える**
   （Unsplash の napi は 2026-08 時点で 401。鍵なしでは使えない）
5. `SLACK_DRAFT.md` の確認8項目を**ユーザーが送る**（★こちらから送信しない）

---

## 9. 守ること（例外なし）

- **数字を捏造しない。** わからない箇所は「※要確認」と書いて空ける
  → 今回、目分量の値を「実測値」と書いてファイルに残した。これが最大の失点
- **メール・フォーム・SNSへの実送信をしない。** 下書きと台帳まで
- **顧客名・金額・個人情報を外部サービスに出さない**
  → SHARE Security の素材は**ローカルのみ**。Claude Design には入れない
- **他社のコードを読まない**（依拠性を作る）。見るのはスクリーンショットだけ
- **頼まれた要素だけで成果物を組む。** 良かれと思った追加は成果物に入れず、チャットで提案する
  → ロゴのタグラインを巨大見出しに昇格させて指摘を受けた実績あり
- **成果物を作ったら `open -R <path>` でFinderに出す**
- 常駐プロセスを `~/Desktop` 配下に置かない（TCCで止まる）

---

## 10. Codex 側で動かすときの注意

- `loop.py` は **CDP over WebSocket**（`websockets` パッケージ）を使う。
  `pip install websockets Pillow` が要る。Python 3.9 で動作確認済み
- `loop.py` は `http://localhost:8788/esl4/fv/<版>.html` を見に行く。
  **サーバーのルートは `~/design-library/docs`**
- 画像の実測は Pillow。`numpy` を使っている箇所もある
- Slack の読み取りは Claude 側の MCP コネクタ経由。Codex 側では使えない可能性が高い。
  **必要な情報は `sharesec/MATERIAL.md` に転記済み**なので、そちらを見れば足りる



======================================================================
# 付録A：CRITERIA.md（発注者の判定基準・全文）
======================================================================

# 判定基準（発注者の目）

ユーザーが実際のLP改修で使っていた判断の型。**すべての実装はこれで検算する。**
出典：本人が過去案件（HookHack LP改修）のやりとりから抽出したもの。

---

## 見るのはこの6つ。順番も重要

| # | 観点 | 実際の言葉 | 裏にある基準 |
|---|---|---|---|
| ① | **見えるか** | 「薄すぎて見えない」「全て薄い色」 | **面と面に段差があるか（輝度差）**。最頻・最再発の指摘 |
| ② | **読めるか** | 「文字と被って読みにくい」 | 装飾がコピーの可読性を**1mmでも下げたら却下** |
| ③ | **自然か** | 「唐突」「自然じゃない感じが残っている」 | 動きが**図形として意識に上がったら失敗**。気配であるべき |
| ④ | **品があるか** | 「気持ち悪い色入れないで」 | **ブランドの色以外を持ち込まない**。既存パレット内で解く |
| ⑤ | **動いていないか** | 「位置を変えるな」 | **合意済みのレイアウトは不可侵** |
| ⑥ | **過剰でないか** | 「使うのは一箇所か多くて二箇所」「boldの必要ない」 | **セクションの【中】の話。**1つのセクション内で、強調（bold・差し色・装飾）を使うのは1〜2箇所まで |

**①が最頻になる構造的理由**：白ベース＋白いカードのLPは、放っておくと**全部が同じ明るさに潰れる**。
好みの問題ではなく構造上の弱点。**「輝度に段差をつける」を最初の共通言語にする。**

---

## 触ってはいけない4つ（案件開始時に1回だけ宣言する）

- **コピー** — 一字一句。英語ラベルの追加も不可
- **構成・並び順** — セクションの順番と配置
- **フォント**
- **要素の増減** — 「良かれと思って」の追加は成果物に入れない（提案はチャットで）

これを固定すると、議論が「デザインが良いか」だけに集中できる。

---

## ベンチマークの使い方（ここが独特で、成果に直結していた）

**「このサイトみたいにして」ではない。**

- **「見た目」ではなく「挙動」を言語化する**
  「おしゃれにして」は解釈が割れる。「**スクロールすると加速する**」は実装が一意に決まる
- **1スクショ＝1機能**に切り分ける → 採用・不採用を個別に判断できる
- 実際、この区間だけ差し戻しがほぼ発生していない

**実例**

| 参照元 | 指定の仕方 | 抜き出した要素 |
|---|---|---|
| Payn | 「画像の裏に、文字が回っている。スクロールすると加速する」 | 背後を流れる巨大文字 |
| Yoom | 「右から左へ永遠スライドしている」 | 無限スライダー |
| デジタルグリッド | 「スクロールするときに、塗る感じで出てくる」 | 線が描かれるストローク |
| SHERPA | 「セクションの箱がズームする感じ」 | グラデ箱のズーム登場 |
| LayerX | 「いきなり背景がブランド色、白いテキスト」 | ダークの句読点セクション |

### 採用したあと、必ず削る

「入れてみる → **全体で見て削る**」を必ず通す。**足す判断と残す判断を分ける。**

- ピン留め → 採用後に外した（強すぎた）
- 無限スライダー → 3×2グリッドに戻した
- ストローク → 2箇所→1箇所、位置も移動
- 巨大文字 → セクション内→セクション間→最後はフッター直前だけ

**強調を使うのは1〜2箇所。** 入れた直後ではなく、通しで見てから判断する。

> 🚨 **これはセクションの中の話。**
> 「このセクションは見せ場／このセクションは脇役」という**ページ全体の強弱づけではない**。
> **セクションが強いか弱いかを考える必要はない。基本は普通でよい。**
> 各セクションは自分の仕事をきちんとやればよく、その【中】で強調を1〜2箇所に抑える。

---

## 差し戻しの3類型

**A. 感覚の申告（原因は言わない）**
「めっちゃ好きだけど、変な感じがする。唐突っていうか」
→ 原因の特定は実装側の仕事。**まず画面を見る。数値だけで判断すると外す。**

**B. 位置・強さの微調整**
「もっと上に置けない？ギリギリ」
→ 数値を1つ動かすだけ。**この型が増えている＝方向は合っている。**

**C. 却下と原状回復**
「一回元に戻していろ」
→ 迷わず戻す。**Cが出るのは実装側が勝手に構造を触った時だけ。**

---

## 往復が伸びた原因（すべて実装側）

1. **見ずに数値で判断した** → 視覚の指摘には**必ず画面を見てから着手する**
2. **頼まれていない構造変更を混ぜた** → 追加要素は**既存要素の位置に影響しない形でしか入れない**
3. **自分で入れた仕組みが他を壊した** → 共通の仕組みを足したら**既存UIを一通り触って確認する**
4. **素材由来の問題を実装で直そうとした** → 直す前に**「素材か、実装か」を切り分ける**

---

## 一言でいうと

> **「良いデザイン」を指示していない。**
> **参照する挙動を渡し、触ってはいけない範囲を決め、違和感を都度申告し、最後に削っている。**



======================================================================
# 付録B：BENCHMARK_METHOD.md（ベンチマークの選び方・全文）
======================================================================

# ベンチマークの選び方 ── 業種ではなく【素材の構成】で選ぶ

案件を問わず使う手順。ESL clubはこれを検証するためのサンプル。

## なぜ業種で選ぶと失敗するか

| 実際に起きたこと | 結果 |
| --- | --- |
| 「余白型ベンチ6本の平均」から色数・余白の基準を出した | その6本は工務店・歯科・Web制作。こども向けに当てて外した |
| 「同じ業種（塾）」で選んだ | 森塾・TOMAS・atama plus はギャラリー非掲載＝質が担保されない |
| 「ギャラリー掲載の良いサイト」で選んだ | 5本中4本が【素材の構成が違う】。当てはめると必ず無理が出る |

**業種が同じでも素材の構成が違えば当てはまらない。**
**構成が同じなら業種が違っても当てはまる。**

## 手順

### ① 手元の素材を数える（素材の棚卸し）

| 項目 | 数える内容 |
| --- | --- |
| 写真 | 枚数／人物か風景か／切り抜けるか／表情は見えるか |
| キーメッセージ | 何行・何文字 |
| 前置き | あるか（1行の説明・実績の言い添え） |
| 注記 | あるか |
| CTA | 何本・主副の区別があるか |
| 数字・実績 | あるか（％・人数・年数） |
| ロゴ | 横長か正方形か・白抜きできるか |
| イラスト・キャラ | あるか（無いなら、それを前提にした型は使えない） |

### ② その構成に一致するFVをギャラリーから探す

- 質の担保＝**MUUUUU.ORG / SANKOU 掲載**（監修あり）
- **業種は問わない**
- 一致とは「写真の枚数」「文字の分量」「数字の有無」が同じこと

### ③ 一致した5本をベンチマークにして、20回回す

## ★構成が一致しない型を使うとどうなるか（実例）

- **UGOKKO型**：ぬいぐるみ6体が前提。ESL clubにキャラは無いので、
  CSSで作った図形で代用 → 「ESL clubがUGOKKOの服を着ている」状態に
- **egaco型**：イラスト多数が前提。写真2枚では画面がもたない
- **College 8型 / UM型**：写真0枚が前提。写真2枚が余る

## 実測：構成一致は極めて少ない（2026-08-21）

MUUUUU掲載 **133本**を機械判定＋実寸で全部確認した結果：

| 条件 | 該当 |
| --- | --- |
| 人物写真2枚＋見出し1本 | **1本（0.75%）** = 028 ひろがり保育プロジェクト |

★**5本は揃わない。** 構成をそのまま条件にすると母数が枯れる。

### 条件の立て方を間違えていた
「人物写真2枚」は、ESL clubの**現状のFVをそのまま条件にした**もの。
だが現状のFVは写真を並べているだけで、**一番強い札（数字の実績）を使っていない**。

条件は「いま何があるか」ではなく「**素材の中で一番強い札は何か**」で立てる。

ESL clubの一番強い札＝合格率5段（100/88/86/64/75%）・累計1,000名・2015年から。
→ 条件は「**見出し＋数字のバッジ＋人物写真**」であるべきだった。

### その条件なら見つかっている（2026-08-21時点）
- **日本速読解力協会**（reading-supporter.jp／MUUUUU掲載）
  見出し＋数字バッジ2つ（25年以上／92%）＋人物写真1枚。**塾・成果を数字で約束・ギャラリー掲載**
- **ウズウズカレッジ**（uzuz-college.jp／MUUUUU掲載）
  見出し＋数字バッジ3つ（15万人／88%／1,500）＋CTA2本＋イラスト

撮影済み133本は scratchpad の `bench64/`（54本）と `rest/`（79本）。
判定スクリプトも同じ場所にある（写真らしい塊の連結成分＋肌色検出）。

## 検証で分かっている事実

- **参照なしだと出力は頭打ちになる。** スイミングを参照なしで20回回したら
  「きれいだが業種から遠い」に収束した。UGOKKOを隣に置いた11回のほうが
  半分の回数で当たった
- **一覧のサムネで判断してはいけない。** 33本を1枚（各300px）で見て、
  5本中3本のラベルと中身を取り違えた。1本ずつ実寸で開くこと
- **コードは読まない。** 他社の computed CSS を読むのは依拠性を作る。
  見るのはスクリーンショットだけ

## 縛りは型によって変わる

級数「6段まで」は余白型（案1 egaco型）で有効だった。
だが**数字を主役にする型では7段になる**。実測で内訳を出したら：

```
50px 見出し / 44px 数字 / 24px 主CTA / 18px 副CTA / 15px ナビ / 13px 単位 / 11px 小見出し
```

無駄な段は1つも無い。**縛りを守るために数字を小さくするのは本末転倒。**
縛りは型ごとに立て直す。守れているかを機械で見るのではなく、
**内訳を列挙して「無駄な段があるか」を見る。**

## 繰り返しの作法（20回回すときのルール）

1. **2つ同時に変えない。** 片方を戻しても原因が残り、回数を無駄にする
2. **犯人を先に実測する。** 推測で直すと外す（実例：7pxの超過を女の子のせいだと
   思って2回外した）
3. **「広げる」「近づける」は必ず衝突を生む**
4. **構造を変えたら、前の指定が届かなくなる**（親が変わると order が無効化される等）
5. **数値と画素は別物。** 「描画されている」と出ても画素を見ると無い場合がある
   （実例：ゴーグルの `<span>` が空で、実測は 164x87 と出た）



======================================================================
# 付録C：SHARE Security 素材棚卸し（全文）
======================================================================

# SHARE Security LP ─ 素材棚卸し

出どころ：Slack `#share-security-project`（2026-08-10〜08-23）と `#適当でいいのだ`。
**ここに書いてあるものだけを使う。無いものは作らない。**
数字・固有名詞で確証が取れないものは「※要確認」を付けて空けてある。

> 🚨 このファイルと成果物は**ローカルのみ**。顧客名・金額を外部サービスに出さない（CLAUDE.md）。
> Claude Design のプロジェクトには入れない。

---

## 1. 誰の何を売るか

| | |
| --- | --- |
| 会社 | SHARE Security社 |
| 担当 | 宮田さん（実名での著者表記・プロフィール掲載は**本人OK済み**） |
| サービス | **脆弱性診断** |
| LPの目的 | **直販の経路をつくる**（現状はパートナー経由：監査法人・システム開発会社・Pマーク取得コンサル） |
| CTAの想定 | 「話を聞きたい」に持っていく。ホワイトペーパーDL → 相談 |

## 2. ターゲット（本人の意向）

- 製薬や金融と取引のある **SaaS系スタートアップ**
- **上場準備（N-2）のCTO**
- ＝ ハイリテラシー層からのインバウンドを狙いたい

**ノンターゲット**（遠藤さんの購買3類型より）
- 「診断の**実施証明**を買う」層
- 「**権威**（会社規模・大手のブランド）を買う」層

## 3. ニーズの起点＝特定のイベント

1. サプライチェーンでの**取引先からの要求**
2. **SaaSベンダーの導入先からの要求**
3. **上場準備での監査対応**
4. 業界内での**セキュリティインシデントの余波**
   （自社で発生した際にガイドライン未達だと信頼が失墜するため、ラインは満たしたいニーズ）

## 4. 強み（そのまま使える事実）

| # | 内容 |
| --- | --- |
| 1 | **網羅性と専門性**：経験豊富な技術者が要件ヒアリングから見積提示、診断実務まで一貫して対応 |
| 2 | **見積提示の工数が少ない**：ヒアリングシートなどの工数は不要。テスト環境の共有のみで対応 |
| 3 | **費用は大手の50%程度** ※「大手が高い」以外の理由は宮田さん確認中 → **要確認** |
| 4 | 診断のコア：**既知パターンをAI、固有システムを人**。**固定2名**が対応することで見積もり工数の簡素化と精度向上。余剰な作業をそぎ落とせるため結果的にコストも下げられる |
| 5 | **出版の実績あり**。Web上での権威性はある |

## 5. 業界構造（LPで語れる一次情報）

- 供給側のAI化は**よくて6割程度**。経験が必要な診断実務に対して**圧倒的に人手が不足**
- GMOが買収を続けて規模を拡大。ただし一定経験のある実務者はその一部で、
  ブランディングにより高額受注している案件でも、一部大手向けを除くと**質の低いアウトプットがまかり通っている**
- 発注者側と受注者側に**情報の非対称性**があり、**納品後も品質判断できない情シス担当が多い**
- 他社の見積もりは「**本数×単価**」（業界慣行）
  ※LPで触れる場合は特定企業への当てつけに読まれないよう**構造説明のトーン**で

## 6. 言い切れる主張（ホワイトペーパー案から）

1. **なぜ脆弱性診断は、ヒアリングシートだけで正確な見積もりが出せないのか**
2. **脆弱性診断がAIで完結しない理由｜攻撃者が突くのは「仕組み」ではなく「システムの思想」**

★2つ目は記事案では「攻撃者が突くのは**設計者の無意識の前提**」とも表現されている。
実際の診断で見つけた事例を**匿名化して1〜2個入れるのは可能**（本人確認済み）。

## 7. 検索で来る人が使う言葉（遠藤さん選定）

| 起点 | キーワード |
| --- | --- |
| 取引先要求 | セキュリティチェックシート 回答 書き方 |
| 上場準備 | 上場審査 セキュリティ 指摘 ／ IPO 脆弱性診断 |
| 監査・認証 | 脆弱性診断 頻度 ／ ISMS 脆弱性診断 |
| 先行着手エリア | **ペネトレーションテスト 脆弱性診断 違い**（競合が比較的緩いことを確認済み） |
| 第二波（後で） | 脆弱性診断 ／ 脆弱性診断 費用 |

## 7.5 ★もう一つの札（2026-08-24 ユーザーから口頭。**未確認**）

> 宮田さんは自宅にサーバーを置いて、**自宅でLLMをローカルで動かせるようにしている**。
> **物理的にセキュリティを確保している**。それを企業に提案できる。

★ユーザー本人が「多分」と付けているので、**事実確認が要る**。
　ただし方向として重要なので記録する。

**これが本当なら、素材の前提が2つ変わる。**

| | 変わること |
| --- | --- |
| 写真 | 「無い」ではなく**サーバーラック／ローカルLLM環境の実物**がある。<br>人物写真ではなく【機材】。暗くて技術的な絵になる |
| 売り物 | 脆弱性診断【だけではない】。<br>**データを外に出さずにAIを使う環境の構築提案**が別の商材として立つ |

**なぜ強いか**
- 「AIを使いたいが、データを外に出せない」企業への解になる
- **自分でやっているから語れる**。買ってきた知識ではない
- Slackにある「業界のAI化はよくて6割」「攻撃者が突くのはシステムの思想」と地続き
- CRITERIA.mdの「2次情報だけで出すな。1次＝本人の直筆・行動記録とセット」に、
  これ以上ないほど合致する

**★LPの分岐（要判断）**

| 案 | LPの主題 |
| --- | --- |
| A | 脆弱性診断のみ（Slackの記録＝確定材料だけで組む） |
| B | ローカルLLM環境の構築提案のみ（新しい札。ただし材料が口頭のみ） |
| C | 両方。「診断で見つける」＋「閉じた環境を作る」の2本立て |

→ Slackの `#share-security-project` は**すべて脆弱性診断の話**で、
　 ローカルLLMの話は一度も出ていない。**別商材の可能性が高い。**
　 どれを作るかで構成が根本から変わるので、先に確定させる。

## 8. ★素材として「無い」もの

LPを組む前提が ESL club とまったく違う。**ここが設計を決める。**

| | |
| --- | --- |
| **写真** | ~~無い~~ → **自宅サーバー／ローカルLLM環境の実物がある可能性**（7.5参照・要確認）。<br>人物写真は未確認 |
| ロゴ | 未入手 → **要確認** |
| サイトURL | Slackに記載なし → **要確認**（「現状サイトのドメイン評価が実質ゼロ」との記述はあるので、サイト自体は存在する） |
| ブランドカラー | 未確認 → **要確認** |
| 実績数字 | 診断件数・年数などは記録に無い → **要確認** |
| 顧客名 | 出せない前提 |

→ **写真ゼロ・数字ゼロで成立するFV**を組む必要がある。
　 ESL clubは「シーン写真2枚＋数字3つ」だった。**構成が真逆**なので、
　 ベンチマークも選び直しになる（素材の構成で選ぶ＝BENCHMARK_METHOD.md）。

## 9. 要確認リスト（宮田さん／橋本さんに聞く）

1. ロゴ・ブランドカラー
2. 現サイトのURL
3. 使える写真（宮田さんの顔写真、作業風景など）
4. 出版物のタイトル・出版社・刊行年（権威性の根拠として出せる）
5. 診断の実績数字（件数・年数・業種）
6. 「大手の50%程度」を言い切ってよいか、その根拠をどう見せるか
7. 匿名化して出せる診断事例（1〜2個）
8. **★自宅サーバー／ローカルLLM環境の件**（7.5）
   ・事実か
   ・写真を撮れるか（ラック、機材、画面）
   ・これは**商材**なのか、脆弱性診断の**説得材料**なのか
   ・企業向けに提案した実績はあるか



======================================================================
# 付録D：Slack下書き（★未送信。送信は本人が行う）
======================================================================

# Slack 下書き ─ #share-security-project

**🚨 下書きのみ。送信はしない。**（CLAUDE.md：実送信は本人が行う）
宛先の想定：`#share-security-project` の hashimoto さん

---

```
@hashimoto
お疲れ様です！
LPの準備を先に進めておきたく、素材で確認したい点をまとめました。
宮田さんに聞くのが早いものと、橋本さんの手元にあるかもしれないものが混ざっています。

◆ すぐ欲しいもの
1. ロゴデータ（SVGかPNG。背景透過があると助かります）
2. ブランドカラー（指定があれば。無ければこちらで提案します）
3. 現サイトのURL

◆ 宮田さん確認が要りそうなもの
4. 使える写真
   ・宮田さんのお顔写真（記事の著者表記でプロフィール掲載OKとのことなので、LPでも使えるか）
   ・診断や作業の風景など、載せられるものがあるか
   ※「無し」なら無しで組みます。写真ゼロ前提のほうが強く作れる型もあるので、判断のためだけに知りたいです

5. 出版物の情報（タイトル／出版社／刊行年）
   「出版の実績もあり、Web上での権威性はある」と伺っている部分です。
   ここはLPで一番強い札になり得るので、書影が使えるかも含めて知りたいです

6. 数字で出せる実績
   診断の累計件数、対応年数、対応業種など。
   1つでもあると構成が変わります。無ければ数字は使わずに組みます

7.「大手の50%程度」の扱い
   ・LP上で言い切ってよいか
   ・言い切る場合、根拠をどう見せるか（"大手が高い"以外の理由を確認中と伺っています）

8. 匿名化して出せる診断事例（1〜2個）
   記事のほうで「実際の診断で見つけた事例を匿名化して入れるのは可能」と
   ご回答いただいていた件、LPでも同じ扱いで大丈夫でしょうか

◆ 補足
数字や事実はこちらで作らない方針なので、埋まらない箇所は空けたまま組みます。
5・6・8が埋まると強度がかなり変わるので、優先度はその3つです。
```

---

## なぜこの8つか

| # | LPのどこに効くか |
| --- | --- |
| 1・2 | 全体のトーン。無ければこちらで提案する（決めの問題） |
| 3 | 既存の言い回し・トーンの確認。作り直しの範囲が決まる |
| 4 | **FVの構成が根本から変わる**。写真アリ／ナシで選ぶベンチマークが違う |
| 5 | **一番強い札になり得る**。「出版の実績」は権威性の一次情報 |
| 6 | 数字があればFVの主役になる。無ければ「言い切れる主張」を主役にする |
| 7 | 価格訴求を出せるかどうか。競合比較の見せ方が変わる |
| 8 | 「攻撃者が突くのは設計者の無意識の前提」の裏付け。これが入ると強い |

## 埋まらなくても進める

素材が無い前提でも組めるように、**言い切れる主張**を主役にした構成で先に作ります。

```
攻撃者が突くのは「仕組み」ではなく「システムの思想」
なぜ脆弱性診断は、ヒアリングシートだけで正確な見積もりが出せないのか
既知パターンをAI、固有システムを人。固定2名で見積工数を簡素化
供給側のAI化はよくて6割。経験の要る診断実務に人手が圧倒的に不足
```

これは全部 Slack の記録にある本人の言葉なので、捏造にならない。
